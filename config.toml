# The URL the site will be built for
base_url = "https://bitdevsLIM.github.io/bitdevsLIM"

title = "BitDevsLIM"
description = "BitDevsLIM es una comunidad en Lima para aquellos interesados en discutir y participar en la investigación y desarrollo de Bitcoin y protocolos y software relacionados."

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

generate_feeds = true
feed_filenames = ["atom.xml", "rss.xml"]

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# Configuration of the Markdown rendering
[markdown]

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Put all your custom variables here

github = "https://github.com/bitdevsLIM/bitdevsLIM"
meetup = "https://evento.so/p/"
discord = ""
